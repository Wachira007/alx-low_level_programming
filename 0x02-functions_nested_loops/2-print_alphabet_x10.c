#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 * return 0 - success
 * */

int main (void)
{
	char letter;
	for letter = 'a'; letter <= 'z'; letter++)
	{
		putchar(letter);
	}
	putchar('\n');
	return (0);
}
